namespace Components.Ast.ExpressionNodes
{
    public enum ExpressionType
    {
        If,
        Binary,
        Unary,
        Relation,
        Factor,
        Constant, 
        Identifier,
        Call
    }
}
